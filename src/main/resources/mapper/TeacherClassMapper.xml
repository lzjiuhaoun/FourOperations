<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.lzj66.dao.TeacherClassesDao">
    <!--目的：为Dao接口方法提供SQL语句-->
    <!--映射实体对象-->
    <insert id="add">
        INSERT INTO teacher_class(teacherId,classId) VALUES (#{teacherId},#{classId})
    </insert>

</mapper>